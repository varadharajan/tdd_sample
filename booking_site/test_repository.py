from unittest import TestCase
from unittest.mock import patch

class TestRepository(TestCase):
	def setUp(self):
		# Ensure we have a clean database
		# db_manager.truncate_all_tables()
		pass

	def test_should_persist_ticket_to_database(self):
		# ticket = Ticket("varadha", 1, "bus 1")
		# repository.persist(ticket)
		# self.assertEquals(ticket, repository.getTicket("varadha"))
		pass